---
description: 桌面端发版 — Agent 发布前必须逐步完成 Checklist（硬性）；版本号、更新日志、标签、onboarding、CI
globs:
  - apps/desktop/package.json
  - apps/desktop/electron/**
  - apps/desktop/scripts/**
  - .github/workflows/release-desktop.yml
  - docs/DESKTOP-RELEASE.md
  - docs/releases/**
  - scripts/assemble-release-notes.mjs
  - client-ui/package.json
alwaysApply: false
---

# 桌面端发版（Agent 硬性流程）

当用户要求 **发布桌面版、打标签、bump 版本、推送 `desktop-v*`、准备 Release** 时，Agent **必须**按本规则与 `docs/DESKTOP-RELEASE.md` **逐步完成 Checklist**，**不得**跳过项、不得凭记忆勾选、不得在必选项未验证前执行 `git tag` / `git push` 标签。

**配套规则**：引导激活参数见 `onboarding.mdc`；schema/用户数据/更新源兼容见 `backward-compatibility.mdc`。

---

## Agent 必须遵守的执行顺序

1. **先 Read** 本文件 + `onboarding.mdc`「发版激活 onboarding」+ `docs/DESKTOP-RELEASE.md` §4–§9 + `docs/releases/README.md`。
2. **建立任务清单**：用 TodoWrite（或向用户列出同等条目）展开下方 **Phase A–F**，逐项 `in_progress` → `completed`。
3. **每项须可验证**：运行命令、读文件或 diff 确认；写「已完成」须附带证据（命令输出摘要 / 文件字段值）。
4. **本地构建 / `check:ui`**：能跑则跑；失败则停止发版并修复，**禁止**带红打标签。
5. **打标签前汇报**：向用户展示 Checklist 完成情况；用户未明确说「直接发布 / 跳过」时，**待确认后再** `git tag` + `git push`。
6. **禁止**：未改 `version` 就打标签；**未撰写更新日志**就打标签；标签名与 `package.json` 不一致；手动改名 `latest-*.yml` 或安装包；无迁移地破坏旧客户端更新链（见 `backward-compatibility.mdc`）。

---

## 更新日志（Release Notes）— 硬性

每次发版 **必须** 在仓库撰写更新日志，由 CI 组装后发布到 **GitHub Release 页面**（非事后手填为主路径）。

| 项 | 说明 |
|----|------|
| **文件路径** | `docs/releases/{version}.md`，`{version}` = `apps/desktop/package.json` 的 `version`（含 `-dev.N`） |
| **模板** | 复制 `docs/releases/TEMPLATE.md` |
| **必需章节** | `## 新功能`、`## 修复`（两节均须存在；无内容写 `- 无`） |
| **文风** | 面向**投资者/用户**：一条一行，说清「能做什么 / 修好了什么」；避免内部文件名、PR 号、技术栈 |
| **与引导关系** | `ONBOARDING_RELEASE_BY_VERSION` 写价值导向亮点；更新日志写**变更清单**（可重叠但引导更短、更营销向） |
| **组装脚本** | `node scripts/assemble-release-notes.mjs {version}` 或 `npm run release:notes` |
| **CI** | `prepare-release` 设 `OPPTRIX_RELEASE_STRICT=1`；缺文件或缺章节 → **workflow 失败** |
| **提交时机** | 更新日志须 **与 version bump 一并 commit 并 push**，再打标签 |

**预览示例**：

```bash
node scripts/assemble-release-notes.mjs 0.6.1 | head -40
```

---

## Phase A — 代码就绪

| # | 检查项 | 验证方式 |
|---|--------|----------|
| A1 | 待发布代码已在目标分支（通常 `main`）且工作区无未提交的意外改动 | `git status`；与用户确认范围 |
| A2 | 若本次含 `client-ui` 改动 | `npm run check:ui` 退出码 0 |
| A3 | 若本次含 `packages` / server 改动 | `npm run build:packages` 无错误 |
| A4 | 发版前本地 UI 构建冒烟 | `npm run build -w opptrix-client` 无错误 |
| **A5** | **桌面打包预检（硬性）** — Updater 依赖可解析、sidecar `deps/` 改名、内置更新根证书、workflow 校验脚本齐全 | **`OPPTRIX_AUDIT_STAGE_UPDATER=1 npm run audit:desktop-pack -w @opptrix/desktop` 退出码 0**（CI 的 `ci.yml` / `release-desktop.yml` 也跑） |

打标签前若 A5 未跑通 → **禁止** `git tag desktop-v*`。

---

## 打包不变量（勿回归 · 历史 CI 踩坑）

| 不变量 | 做错的症状 | 门禁 |
|--------|------------|------|
| Updater 路径 **不得** 含名为 `node_modules` 的目录段 | 「更新组件不可用」 | `stage-updater-deps` → `build/updater-deps/packages/`；`verify-packaged-updater` |
| 嵌套依赖（如 `fs-extra`）须从 **父包目录** 解析，且桌面包声明 `fs-extra` | CI：`electron-updater dependency missing: fs-extra` | `audit:desktop-pack` + `OPPTRIX_AUDIT_STAGE_UPDATER=1` |
| Sidecar 依赖目录为 `runtime-stage/deps/`（非顶层 `node_modules`） | 安装后 sidecar 起不来 / 缺 Fastify | `stage-runtime` 改名；`verify-packaged-runtime` |
| Windows 更新：自签 Authenticode + 内置 CA + `update-signature` 自定义验签 | 「not signed by the application owner」 | secrets `OPPTRIX_CODE_SIGNING_*`；release audit `REQUIRE_SIGN_SECRETS` |
| 公证包内不得留 `.cache/prebuilds` 未签名 `.node` | macOS notarization 失败 | `extraResources` filter `!**/.cache` |
| `prebuild` / `ci.yml` / `release-desktop.yml` 均跑 `audit-desktop-pack` | 静态回归漏到 90min 构建后才炸 | 本规则 A5 |

---

## Phase B — 版本号、更新日志与 onboarding 激活

| # | 检查项 | 验证方式 |
|---|--------|----------|
| B1 | **`apps/desktop/package.json` `version` 已 bump** 为本次发布号（语义化版本；真源） | `node -p "require('./apps/desktop/package.json').version"` |
| B2 | 若同步发 Web UI：**`client-ui/package.json` `version` 已 bump** | 读文件或与桌面版对齐 |
| B3 | **引导亮点**：`ONBOARDING_RELEASE_BY_VERSION` 已添加/更新**与新版本前缀匹配**的键 | 读 `client-ui/src/onboarding/manifest.ts` |
| B4 | 若改版引导 UI/步骤：`ONBOARDING_FLOW_VERSION` 已在 **`packages/shared/src/onboarding.ts` 与 `client-ui/src/onboarding/constants.ts` 同步 bump** | 两文件同值 |
| B5 | 若协议变更：`LEGAL_AGREEMENTS_VERSION` 两处同步 bump；`aboutLinks.ts` URL 已核对 | 两文件同值 |
| B6 | **更新日志** `docs/releases/{version}.md` 已撰写，含 `## 新功能` 与 `## 修复` | 读文件；`OPPTRIX_RELEASE_STRICT=1 node scripts/assemble-release-notes.mjs {version}` 退出码 0 |
| B7 | 版本号、更新日志、onboarding 相关变更已 **commit** | `git log -1` 含对应文件 |

细则与决策表见 `onboarding.mdc` §发版激活 onboarding。

---

## Phase C — 兼容性与已知问题（按需）

| # | 检查项 | 何时必做 |
|---|--------|----------|
| C1 | SQLite / 用户 preference / 本地 JSON 格式变更已有 **幂等迁移** | 动过 schema 或存储格式 |
| C2 | Hub/API 破坏性变更已版本化或双读；旧客户端可升级或收到明确提示 | 动过 API 契约 |
| C3 | `app-update.yml` / R2 / CDN 变更不会使**已发布旧版**永久无法更新 | 动过更新源 |
| C4 | 更新日志「修复」节已覆盖用户可见的回归修复；重大限制写在「新功能」后或修复节末 **一句**说明 | 有破坏性/手动步骤时 |
| C5 | 若升级 Electron：`build.electronVersion` 已更新且三端冒烟计划已说明 | 升 Electron 时 |
| **C6** | **Windows 更新签名**：`OPPTRIX_CODE_SIGNING_P12`（或 `WIN_CSC_LINK`）已配置；客户端内置根 CA + 自定义 `verifyUpdateCodeSignature`；`publisherName` 与叶证 CN（`Opptrix`）一致 | 自动更新 / Authenticode；未配置则 `audit:desktop-pack` 在 release 会失败 |
| **C7** | **安装包依赖齐全**：sidecar `runtime-stage/deps`（非 `node_modules`）；updater 在 `build/updater-deps/packages`（含嵌套 `fs-extra` 等） | 已知 CI 踩坑：EB 跳过顶层 `node_modules`；`stage-updater-deps` 须从 parent 解析嵌套依赖 |

---

## Phase D — 打标签前最后确认

| # | 检查项 | 验证方式 |
|---|--------|----------|
| D1 | 计划标签名 = `desktop-v{version}`，且 `{version}` **等于** `apps/desktop/package.json` `version` | 字符串完全一致 |
| D2 | 该标签尚未存在 | `git tag -l 'desktop-v*' \| grep 目标标签` 为空 |
| D3 | `docs/releases/{version}.md` 已 push 到远程 `main` | `git log origin/main -- docs/releases/{version}.md` |
| D4 | 已向用户汇报 Phase A–C（及 D）完成情况 | 对话中可见 |

**打标签命令模板**（版本示例 `0.6.1`，以 B1 实测为准）：

```bash
node -p "require('./apps/desktop/package.json').version"
OPPTRIX_RELEASE_STRICT=1 node scripts/assemble-release-notes.mjs "$(node -p "require('./apps/desktop/package.json').version")" | head -30
git push origin main
git tag desktop-v0.6.1
git push origin desktop-v0.6.1
```

---

## Phase E — CI 与产物（推送标签后）

| # | 检查项 | 验证方式 |
|---|--------|----------|
| E1 | GitHub Actions `release-desktop` workflow 各 job 成功 | `gh run list` / Actions UI |
| E2 | Release 附件含 Mac x64+arm64（dmg/zip）、Windows exe、Linux AppImage/deb 及对应 **`latest-*.yml`** | `gh release view desktop-v{version}` |
| E3 | `verify-packaged-updater` **与** `verify-packaged-runtime` 通过；release 开头 `audit-desktop-pack` 通过 | CI 日志 |
| E4 | **GitHub Release 正文**含「新功能」「修复」及安装说明 | `gh release view desktop-v{version}` 或网页核对 |
| E5 | R2 同步 job 成功（若启用） | CI `sync-r2` |
| E6 | Windows 安装包具备 Authenticode（自签或商业）；若用自签，应用内更新验签不依赖系统 Status=Valid | 产物 + 客户端日志 |

若 Release 已存在但正文缺更新日志（罕见）：用 `node scripts/assemble-release-notes.mjs {version} > /tmp/notes.md` 后 `gh release edit desktop-v{version} --notes-file /tmp/notes.md` 修正，并排查为何打标签前未提交 `docs/releases/{version}.md`。

**禁止**手动改名 electron-builder 产物或 yml 内 `url`/`version`。

---

## Phase F — 发版后冒烟（建议，用户要求时必做）

| # | 检查项 |
|---|--------|
| F1 | 在至少一个目标平台：旧版安装 → 检查更新 → 下载 → 重启 → 新版本启动 |
| F2 | 升级后 **onboarding** 按预期出现（版本 bump 或 flow/agreements bump） |
| F3 | 核心路径：登录/会话、LLM 配置、行情、聊天可用了 |

本地重置 onboarding 测引导：`onboarding.mdc` §本地验证 `curl` 示例。

---

## 快速命令索引

```bash
# 当前桌面版本（真源）
node -p "require('./apps/desktop/package.json').version"

# 预览将发布的 Release 正文
npm run release:notes

# 发版前构建 + 打包预检（硬性，同 CI）
npm run build:packages
npm run check:ui
npm run build -w opptrix-client
OPPTRIX_AUDIT_STAGE_UPDATER=1 npm run audit:desktop-pack -w @opptrix/desktop

# 已有桌面标签
git tag -l 'desktop-v*' --sort=-v:refname | head

# 查看 Release
gh release view desktop-vX.Y.Z
```

完整说明、产物命名、FAQ：`docs/DESKTOP-RELEASE.md`。

## 禁止（反模式）

- ❌ 未 Read 本规则与用户确认就 `git push` 标签
- ❌ **未撰写** `docs/releases/{version}.md` 就打标签（CI 将失败或 Release 无变更说明）
- ❌ 更新日志只有安装说明、缺少「新功能 / 修复」清单
- ❌ Checklist 未逐项验证就宣称「可以发布」
- ❌ **跳过 `audit:desktop-pack`** 或已知 EB/`fs-extra`/sidecar `deps` 坑未验收就打标签
- ❌ 只改文案不 bump `version` / `ONBOARDING_*` / `LEGAL_*` 却期望老用户看到新引导
- ❌ `desktop-v*` 与 `package.json` version 不一致
- ❌ 修改已发布 Release 的 yml 糊弄版本；应新发正确版本并在更新日志说明
