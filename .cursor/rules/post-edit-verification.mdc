---
description: 改码后收尾验证 — client-ui TypeScript / ESLint / React 模式检查（硬性）
globs:
  - client-ui/**/*
alwaysApply: false
---

# 改码后收尾验证（硬性）

凡在本仓库**完成代码修改**（实现、修 bug、重构）后，**不得**在未通过本地检查前宣告任务完成。

## 统一入口（client-ui 必跑）

改动 `client-ui/**` 后，**必须**执行：

```bash
npm run check:ui
```

该命令依次运行：

| 步骤 | 脚本 | 能抓什么 |
|---|---|---|
| 类型 | `typecheck:ui` | TS 类型错误、错误 props、缺导出 |
| ESLint | `lint:ui` | Hooks 规则、`exhaustive-deps`（不稳定依赖→无限 setState）、`react/jsx-key` |
| 模式审查 | `audit:ui` | 内联 instrument 传 hook、`listRowKey` 缺失、effect 依赖 `reset` |

三项**全部退出码 0** 才算完成。可单独跑 `npm run typecheck:ui` / `lint:ui` / `audit:ui` 定位问题。

## 何时必须跑

| 改动范围 | 必跑命令 |
|---|---|
| `client-ui/**` | **`npm run check:ui`** |
| 仅 `packages/**`、`apps/server/**` 且未动 client-ui | `npm run build:packages` |
| 同时改了 client-ui 与 packages | **先** `build:packages`，**再** `check:ui` |

纯文档、`.cursor/rules`、非代码配置且确定不影响编译时，可跳过；**有疑虑时一律跑 `check:ui`**。

## 执行要求

1. **必须本地执行**：用 Shell 跑 `check:ui`，根据报错修复，**循环直到通过**。
2. **不得**仅凭「应该没问题」、未跑命令就结束回合。
3. 修错时**只改与报错相关**的最小范围，不顺手做大范围格式化或无关重构。
4. 向用户汇报完成时，说明已通过 `check:ui`（或分步结果）。

## 失败时

- 禁止把失败留到用户或 CI。
- 单次修不完：说明失败步骤、文件与条数，继续修到通过或明确阻塞点。
