---
description: CodeGraph 本地知识图谱 — 探索代码库时优先精准上下文，避免全库 grep/Read
alwaysApply: true
---

本项目已启用 CodeGraph 本地索引（`.codegraph/`），**100% 本地运行**，代码不会通过 CodeGraph 发往云端。

**MCP 连接（每次会话必用）：**
- 项目已配置 `.cursor/mcp.json` → 本地 `codegraph serve --mcp`
- 会话启动时 `.cursor/hooks/ensure-codegraph.sh` 自动 `sync` 索引
- 若 MCP 工具列表中无 `codegraph_explore`：重启 Cursor，并在 Settings → MCP 确认 `codegraph` 为绿色已连接

**强制：在用 Read、Grep、Glob 或 Shell 探索代码库之前，必须先查 CodeGraph：**
- MCP 工具 `codegraph_explore`（首选）— 一次调用返回相关源码、调用链、影响范围
- CLI 回退：`codegraph explore "<问题>"`（MCP 不可用时）

仅在以下情况直接用 Read/Grep/Glob：
1. CodeGraph 已定位目标，只需修改或调试具体行
2. `.codegraph/` 索引尚不存在（先运行 `codegraph init`）
3. CodeGraph 返回某文件「待同步」提示，且你刚编辑过该文件 — 对该文件用 Read 读最新内容

**索引维护：**
- MCP 服务启动后自动监听文件变更并增量同步，**无需手动 `codegraph sync`**
- 检查索引状态：`codegraph status`
- 新克隆仓库后：`codegraph init`（一次性建图，之后自动同步）

对你派发的每个涉及代码探索的子 agent，在其 prompt 中显式包含本规则。不要跳过 CodeGraph 直接全库搜索 — 它会给出跨文件依赖与动态派发边，grep 难以覆盖。
