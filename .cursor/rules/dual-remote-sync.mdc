---
description: GitHub + Gitee 双远程同步 — 每次 push / merge 后必须两端一致
alwaysApply: true
---

# 双远程同步（GitHub + Gitee）

## 核心原则

本仓库同时维护 **GitHub** 与 **Gitee**。凡用户要求 **push**、**merge**、**merge 后 push**，Agent **必须**使两端同一分支 tip 一致；禁止只更新一侧就汇报完成。

## 远程约定

| Remote | 用途 |
|--------|------|
| `origin` | fetch = GitHub；pushurl 含 GitHub + Gitee（`git push origin …` 通常双写） |
| `gitee` | 仅 Gitee；用于 `gh pr merge` 后补齐、以及显式校验 |

以 `git remote -v` 为准；若缺 `gitee` 或 `origin` 无 Gitee pushurl，先补齐再推，勿静默跳过。

## 强制流程

### 1. 推送功能分支

```bash
git push -u origin HEAD
# 若 origin 未双写 Gitee，再执行：
git push -u gitee HEAD
```

### 2. `gh pr merge` 之后（硬性）

`gh` 只更新 **GitHub**。合并成功后 **必须立刻**：

```bash
git fetch origin main
git checkout main && git pull origin main
git push gitee main
git push gitee --delete <feature-branch>   # 若 Gitee 仍有该分支
# 可选：git push origin --delete <feature-branch> 若 GitHub 未自动删
```

### 3. 直接推 main / tag

```bash
git push origin main          # 依赖双 pushurl
git push gitee main           # 仍建议显式推一次，防配置漂移
git push origin <tag> && git push gitee <tag>   # 发版标签两端都要有
```

### 4. 完成前校验（必做）

```bash
git fetch origin main && git fetch gitee main
test "$(git rev-parse origin/main)" = "$(git rev-parse gitee/main)"
```

两端 SHA 不一致 → **未完成**，继续推送直至一致后再向用户汇报。

## 汇报要求

交付 push/merge 结果时写明：

- GitHub：已更新（PR/commit/SHA）
- Gitee：已同步（`main` 或分支 SHA 与 GitHub 一致）

## 反模式

- ❌ 只 `gh pr merge` 就说「已 merge」，不推 Gitee `main`
- ❌ 只推 GitHub 功能分支，Gitee 无对应分支或 tip 落后
- ❌ 发版只推一侧 tag
- ❌ 不校验两端 `main` SHA 就结束
