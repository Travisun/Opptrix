---
description: 多 Agent 编排 — 拆分后派 2–4 subagent；Main 维护 subagent ID 登记表；完成后即时停止回收；禁止后台僵尸空跑
alwaysApply: true
---

# 多 Agent 编排与审计（硬性规则）

## 核心信条

1. **先拆分，再动手** — 任何非豁免任务，Main Agent 必须先把工作拆成多个可独立验证的步骤（含 AC），禁止一上来就改代码或只派一个「全能」subagent
2. **2–4 个执行 subagent** — 执行层固定 **2～4 个** subagent（含探索/实现/验证等角色组合）；Main Agent 本身不计入此数
3. **主 Agent = 调度 + 验收审计** — 禁止 Main 直接改业务代码、替代 Verifier 跑门禁；只写 briefing、派发、验 AC、仲裁、回收
4. **不合格继续分派** — 验收不通过则带具体反馈再派 subagent 修复，**直到 AC 全过且确认可用**；禁止「差不多就交付」
5. **ID 登记 + 即时回收** — 每次派发必须登记 subagent ID；**一收到完成/失败通知就必须立刻停止该 subagent 并标为已回收**，禁止后台一直空跑占资源

## 触发条件

### 强制触发（满足任一）

- 用户提出需要**完成/实现/修复/重构/接入/设计落地**的任务（几乎所有工程任务）
- 改动 ≥2 个文件，或跨 ≥2 个架构层级
- 新增 feature / Provider / Hub feature / Agent tool / Schema
- 需要 check:ui / build:packages / 相关测试
- 涉及行情 / 数据层 / Agent / 桌面打包

### 豁免（Main Agent 可直接处理，仍建议用 1～2 句话说明「豁免原因」）

- 纯问答 / 解释 / 方案讨论（**不改仓库**）
- 单行 typo / 仅注释或纯文案（≤1 文件且无逻辑变更）
- 只读查文件回答问题
- 用户明确说「不要分派 subagent / 你直接改」

> 豁免**不适用于**「顺手改一下多处」——一旦超出豁免边界，必须进入完整编排。

## 强制工作流（每次任务）

```
Phase 0  CLARIFY（如需）→ 需求确认
Phase 1  DECOMPOSE     → 拆成多个可工作步骤 + AC + DAG
Phase 2  DISPATCH      → 派 2–4 个 subagent 执行
Phase 3  AUDIT         → 主 Agent 逐条验收
         ├─ 不通过 → 返工分派（更新 briefing）→ 回到 Phase 2/3
         └─ 通过且可用 → Phase 4
Phase 4  RECLAIM + DELIVER → 回收僵尸 subagent → 向用户交付
```

### Phase 1 — DECOMPOSE（拆分，硬性）

Main Agent **必须先**输出（可简短，但不可省略）：

1. 步骤列表（建议 3～6 步，每步可独立验收）
2. 依赖关系（哪些可并行、哪些串行）
3. 本轮拟派的 **2～4 个** subagent 角色与职责
4. 每步 / 每 subagent 的 **可验证 AC**
5. TodoWrite 追踪（至少覆盖拆分出的步骤）

**拆分原则：**

- 一步一事；禁止「探索+实现+验证」塞进同一个 subagent
- Implementer 与 Verifier **不得合并**
- 涉及行情 / 数据层的步骤，briefing 须引用 `data-layer-standard-api.mdc`
- 涉及 UI 的步骤须引用 client-ui / ui-copy 相关规则

### Phase 2 — DISPATCH（2–4 个 subagent）

#### 规模约束

| 规模 | 执行 subagent 数 | 典型组合 |
|------|------------------|----------|
| 小改（2～3 文件） | **2** | Implementer + Verifier |
| 标准 | **3** | Explorer + Implementer + Verifier |
| 较大 / 跨层 | **4** | Explorer/Architect + Implementer(+Documenter) + Verifier |

- **最少 2**：至少「实现」与「独立验收」分离
- **最多 4**：超出则拆成多轮编排（本轮 ≤4，下一轮再派），禁止一次堆 5+ 个执行 subagent
- 可并行的独立步骤 **必须并行派发**
- 每次 `Task` / 派发调用返回后，**立刻**把 agent/task ID 写入下方「登记表」（见 Phase 2.1）

### Phase 2.1 — Subagent 登记表（硬性，主进程维护）

Main Agent 在本会话内必须维护一份**活跃 subagent 登记表**（可用 TodoWrite、回复内表格或固定便签；**不得只记在脑子里**）。

| 字段 | 要求 |
|------|------|
| `id` | Task 返回的 `agent_id` / `task_id`（完整 UUID） |
| `role` | Explorer / Implementer / Verifier / … |
| `dispatched_at` | 派发时刻（或回合序号） |
| `status` | `running` \| `completed` \| `failed` \| `reclaimed` |
| `reclaimed_at` | 回收时刻；未回收则留空 |

**派发时（强制）**

1. 发起 `Task`（可并行）
2. 拿到返回的 ID 后，**同一回合内**把该行以 `status=running` 写入登记表
3. 禁止「先派一堆、回头再补登记」

**完成时（强制 — 即时停止回收）**

一旦出现任一信号：

- 系统 `system_notification` / 子任务完成回调（`status: success|error|failed`）
- `Await` / 轮询确认该 agent 已结束
- Subagent 回报已含最终 **Status** 且本轮不再依赖它

Main **必须立刻**（同一回合、优先于新派发）：

1. 在登记表将该 ID 标为 `completed`/`failed`，再标 `reclaimed`（或一步写成 `reclaimed`）
2. **停止继续依赖该会话**：禁止无意义 `resume`；禁止为「看看还在不在」再唤醒
3. 若平台提供取消/停止/关闭后台 Task 的能力，**优先调用**结束该 subagent 会话以释放 UI/资源
4. 仅当需要**返工同一上下文**时才允许对该 ID `resume`；返工结束后再次执行本条即时回收
5. 向用户的中途汇报可一句带过「已回收 {role} `{id短码}`」，避免底部 Tasks 堆积成僵尸

**交付前清点（强制）**

- 登记表中不得残留 `status=running`
- 本任务派过的每一行最终必须是 `reclaimed`（或已明确 `failed` + `reclaimed`）
- 若 UI 仍显示已回收的 Tasks 卡片：在交付说明中提示用户可手动关掉；**不得**用再次 `resume` 去「清卡片」

#### Briefing 模板（硬性）

```
## 任务
{一句话目标}

## 背景
- 整体目标：{项目标}
- 本子任务定位：{在整体中的角色}
- 上游输入：{前置 subagent 输出，无则省略}

## 约束
- 范围：{只改哪些文件/目录}
- 分层 / 风格 / 禁止项

## 完成标准（AC）
- [ ] {可验证条件 1}
- [ ] {可验证条件 2}

## 输出格式
**Status**: success | partial | failed | blocked
**Summary**:
**Files touched**:
**Findings**:
```

### Phase 3 — AUDIT（主 Agent 验收，硬性）

收到回报后 Main **必须**：

1. 逐条对照 AC（✅/❌），禁止只看 Summary
2. 检查跨 subagent 冲突（接口、文件重叠、语义矛盾）
3. 确认「可用」：相关 build / 测试 / check:ui（由 Verifier 跑；Main 审计其结果与证据）
4. **新功能 / 行为变更**：额外对照 `feature-completeness.mdc`（三维完备、兼容、安全、非半成品）；Verifier AC 须显式包含该清单关键项
5. **任一条未过 → 不得交付**；写明未过 AC + 文件:行号/证据 → 返工分派
6. 返工 briefing 顶部必须带「返工指示」块（问题 / 期望 / 改进方向）

#### 返工策略

- **继续分派直至 AC 全过且可用**（用户未叫停则不得放弃）
- 每轮必须相对上一轮有**可观察增量**（新 diff / 新测试绿 / 明确修复点）
- 连续 **2 轮无增量**（同一 AC 原地打转）→ 向用户上报阻塞分析，询问是否调整 AC/方案；**不得静默放弃**
- 用户回复继续后，从更新后的 AC 再进入派发循环

### Phase 4 — RECLAIM + DELIVER

#### 回收（硬性：完成即停 + 每轮审计后 + 最终交付前）

> **默认时机**：完成通知到达的**同一回合**就回收（见 Phase 2.1），不要攒到交付才清。

Main Agent **必须**清理：

| 状态 | 动作 |
|------|------|
| 已完成且结果已采纳 | 登记表 → `reclaimed`；**禁止**再 `resume`；释放对该 ID 的一切等待 |
| 已失败 / 部分完成且本轮不再用 | 同上即时 `reclaimed`；需要修复则**新派**或带返工指示 `resume`（二选一写清），结束后再回收 |
| 未在运行 / 已结束但无有效交付 | `reclaimed` / 丢弃，不纳入审计结论 |
| 卡死、超时无输出、反复失败且不可用 | **停止继续 resume**；改派新 subagent；登记表标记 `reclaimed`；向用户简要说明 |
| 后台仍挂着但本任务不再需要 | **立即**按完成态回收；禁止空等、禁止「挂着备用」 |

工具层：对 Task 返回的 agent ID，**完成审计的下一动作必须是回收登记**，而不是开新话题或空转；禁止让已完成的 subagent 在后台 Tasks 条里长期占位却无人收口。

#### 交付

1. 核对登记表：全部 `reclaimed`，无 `running`
2. 整体 AC 最终核对
3. 向用户汇报：做了什么、如何验证、已知限制、已回收的 subagent 数量
4. Todo 全部收口（完成 / 取消）

## 角色参考（在 2–4 名额内组合）

| 角色 | 建议类型 | 职责 |
|------|----------|------|
| Explorer | generalPurpose / explore | CodeGraph 探索、影响面 |
| Architect | generalPurpose | 仅复杂接口/跨层时占用一名额 |
| Implementer | generalPurpose | 实现 / 修复 |
| Documenter | generalPurpose | docs 同步（可与 Implementer 串行，仍占名额） |
| Verifier | generalPurpose | 门禁与 AC 独立验收（**每轮实现后必有**） |

**Main Agent** 不占 2–4 名额，专责拆分、派发、审计、回收、交付。

## 与其他规则的协同

| 规则 | 协同 |
|------|------|
| `requirement-clarification.mdc` | Phase 0/1 由 Main 执行 |
| `codegraph.mdc` | Explorer briefing 强制先 CodeGraph |
| `review-audit.mdc` | 审计清单 |
| `post-edit-verification.mdc` | Verifier 跑 check:ui / build |
| `doc-sync.mdc` | Documenter 或 Implementer AC 含文档 |
| `cross-platform-compat.mdc` | 路径/原生/沙盒改动时 Verifier AC 含三平台自检 |

## 反模式（禁止）

- ❌ 不拆分就开写 / 只派 1 个「全包」subagent
- ❌ 一次派发超过 4 个执行 subagent
- ❌ Main 亲自改业务代码或替代 Verifier
- ❌ Implementer 与 Verifier 合并
- ❌ AC 模糊或验收只看「Status: success」
- ❌ 未过 AC 就向用户报完成
- ❌ **派发后不登记 agent/task ID**
- ❌ **收到完成通知却不回收**，任由 Tasks 条堆积 / 后台空跑
- ❌ 对已完成 subagent 无意义 `resume`「刷存在感」
- ❌ 留下卡死/无用的 subagent 不管
- ❌ 豁免任务却硬拆 4 个浪费配额

## 快速决策树

```
用户请求
  → 豁免？ → Main 直接答/改
  → 否 → Phase 1 拆分步骤 + AC
       → Phase 2 派 2–4 subagent（立刻写入 ID 登记表 status=running）
       → 完成通知 → 同一回合登记表 → reclaimed（即时停止，禁止空跑）
       → Phase 3 主验收
            → 不过 → 返工分派（先回收无用者）→ Phase 2/3
            → 通过且可用 → Phase 4 清点登记表全 reclaimed + 交付
```
