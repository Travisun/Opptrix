---
description: UI 文案规范 — 所有面向用户的文案必须为产品级高端风格，禁止任何技术描述；统一 onboarding、按钮、空状态、报错、设置等全部 UI 文案标准
alwaysApply: true
---

# UI 文案规范（硬性规则，全仓生效）

## 核心原则

**每一个出现在用户界面上的字，都在定义产品的品质感。**

1. **用户第一**：写给使用者，非开发者。用户不关心实现，只关心「我能做什么」
2. **结果导向**：描述用户能达成的结果，而非系统行为
3. **简洁有力**：一句能说清不写两句；删掉所有不增加信息量的字
4. **日常自然**：像一位专业顾问在对话，不像说明书在朗读
5. **价值感**：让用户感受到「这产品懂我、替我着想」

## 适用范围

**所有用户可见的文案**，包括但不限于：

| 场景 | 示例 |
|------|------|
| Onboarding 引导 | 欢迎语、步骤标题、功能亮点、引导说明 |
| 按钮 / 操作 | 主按钮、次按钮、图标按钮、行内操作 |
| 标题 / 标签 | 页面标题、区块标题、Tab 标签、分组标题 |
| 空状态 | 无数据、无结果、未配置、首次使用 |
| 错误 / 失败 | 网络错误、加载失败、校验失败、权限不足 |
| 成功 / 完成 | 保存成功、配置完成、任务结束 |
| 确认 / 警告 | 删除确认、不可逆操作、离开提示 |
| 加载 / 等待 | 加载中、处理中、正在保存 |
| 设置 / 表单 | 字段标签、帮助文本、占位符、校验提示 |
| 通知 / Toast | 成功提示、错误提示、信息通知 |
| 对话 / Agent | 系统提示（用户可见部分）、功能引导 |
| 更新日志 | Release Notes、版本亮点 |

## 禁止（硬性）

### ❌ 禁止技术术语

| 禁止 | 原因 | 替代思路 |
|------|------|----------|
| API / API Key / Token | 用户不需要知道接口概念 | 用「数据源」「行情源」或直接说功能 |
| MCP / hydrate / F10 | 内部代号或技术缩写 | 用业务含义替代 |
| Provider / Engine / Hub | 架构层概念 | 用「数据源」「服务」 |
| 数据库 / SQLite / Schema | 存储实现 | 用「本地数据」「你的配置」 |
| 缓存 / 同步 / 请求 | 技术行为 | 用「加载」「刷新」「获取」 |
| 组件名（OpptrixButton 等） | 代码标识 | 永远不出现在 UI |
| 文件名 / 路径 | 实现细节 | 永远不出现在 UI |
| PR / commit / 分支 | 开发流程 | 永远不出现在 UI |
| 错误码（HTTP 500 等） | 技术诊断 | 用「服务暂时不可用」 |
| 函数名 / 变量名 | 代码标识 | 永远不出现在 UI |

### ❌ 禁止技术式描述

| ❌ 禁止 | ✅ 替代 |
|---------|---------|
| 请求超时，请检查网络连接 | 加载超时，请确认网络后重试 |
| 数据拉取失败 | 暂时无法获取数据 |
| 正在同步行情数据 | 正在获取最新行情 |
| 本地缓存已清除 | 已清除本地数据 |
| 配置已持久化 | 配置已保存 |
| 正在建立 WebSocket 连接 | 正在连接服务 |
| 服务端异常（Error 500） | 服务暂时不可用 |
| 参数校验失败 | 请检查输入内容 |
| 正在执行迁移脚本 | 正在更新本地数据 |
| 数据格式不匹配 | 文件格式不支持 |

### ❌ 禁止无信息量的废话

| ❌ 禁止 | ✅ 替代 |
|---------|---------|
| 操作成功 | （不需要提示，或具体说明做了什么） |
| 确定要删除吗？ | 删除后无法恢复，确定继续？ |
| 加载中... | 正在加载行情...（说明在加载什么） |
| 暂无数据 | 还没有关注任何标的（说明为什么 + 下一步） |
| 出错了 | 加载失败，请稍后重试（说明可做什么） |
| 功能开发中 | 即将上线，敬请期待（给预期） |

### ❌ 禁止暴露实现细节

| ❌ 禁止 | ✅ 替代 |
|---------|---------|
| 从腾讯行情源获取 | 获取最新行情 |
| 使用新浪数据 | 数据来源已更新 |
| 已切换至备用 Provider | 已切换至备用数据源 |
| 本地 SQLite 存储 | 本地存储 |
| 经 MCP 工具路由 | （不提，用户不关心路由） |

## 写作标准

### 1. Onboarding 引导文案

- **价值优先**：第一句话就让用户知道「这能帮我做什么」
- **一句一义**：每句话只传递一个信息点
- **动词开头**：用动作引导用户
- **给预期**：告诉用户需要多久、几步

```
❌ "配置 API Key 以接入行情数据源"
✅ "添加数据源，开始查看实时行情"

❌ "本步骤将初始化本地数据库并注册 Provider"
✅ "完成基础配置，约需 1 分钟"

❌ "MCP 工具包路由已就绪"
✅ "智能投研助手已准备就绪"
```

### 2. 按钮 / 操作文案

- **动词优先**：用动作词让用户知道会发生什么
- **结果明确**：用户看完就知道点了会怎样
- **简短**：1-4 个字为佳

```
❌ "提交"          ✅ "保存配置"
❌ "执行"          ✅ "开始分析"
❌ "确认"          ✅ "删除标的"
❌ "取消关注"      ✅ "取消关注"（OK，动词+对象）
```

### 3. 空状态文案

必须回答两个问题：**为什么没有** + **下一步是什么**

```
❌ "暂无数据"
✅ "还没有关注任何标的
    搜索并添加你感兴趣的标的，开始跟踪行情"

❌ "加载失败"
✅ "暂时无法加载行情
    请检查网络后重试，或切换数据源"

❌ "未配置"
✅ "还没有配置数据源
    完成配置后即可查看实时行情，约需 1 分钟"
```

### 4. 错误 / 失败文案

必须包含：**发生了什么** + **可做什么**

```
❌ "网络请求失败"
✅ "网络连接不稳定
    请检查网络后重试，或稍后再试"

❌ "服务器错误"
✅ "服务暂时不可用
    请稍后重试，你的数据不会丢失"

❌ "参数错误"
✅ "输入内容有误
    请检查后重新输入"
```

### 5. 加载 / 等待文案

- 说明**正在做什么**（而非技术行为）
- 耗时操作给**预期**

```
❌ "加载中..."
✅ "正在获取最新行情..."

❌ "正在处理..."
✅ "正在生成投研报告，预计需要 1-2 分钟"

❌ "同步中..."
✅ "正在同步你的配置..."
```

### 6. 确认 / 警告文案

- 说明**后果** + **是否可逆**
- 危险操作用 `confirmTone="danger"`

```
❌ "确定删除？"
✅ "删除后将无法恢复，确定继续？"

⚠️ 注意：这里「无法恢复」比「永久删除」更自然，
   但如果是可恢复的，应改为「删除后可在 30 天内恢复」
```

### 7. 设置 / 表单文案

- **标签**：名词或名词短语，1-6 字
- **帮助文本**：补充说明，非必填时说明原因
- **占位符**：示例或提示，非校验规则

```
❌ "API Key（用于身份验证的令牌）"
✅ "数据密钥（可选，用于接入高级行情）"

❌ "请输入有效的 ts_code 格式"
✅ "请输入 6 位股票代码，如 600519"
```

## 语气与风格

### 人称

- 用「你」不用「您」（亲近但不失礼）
- 用「我们」描述产品行为（「我们已为你保存」）
- 避免「该用户」「该标的」等第三人称

### 语气

- **专业但不冷漠**：像一位懂行的朋友
- **自信但不绝对**：用「通常」「一般」而非「一定」「必须」
- **积极但不浮夸**：避免「震撼」「颠覆」「完美」

### 节奏

- 短句为主，一句不超过 20 字
- 重要信息放前面
- 否定句改肯定句（「请填写必填项」→「请补充完整后继续」）

## 质量检查清单

修改或新增 UI 文案时，逐项检查：

- [ ] 用户能否在 3 秒内理解含义？
- [ ] 是否包含技术术语或内部代号？
- [ ] 是否描述了用户能做什么/看到什么？
- [ ] 是否一句能说清？有无冗余？
- [ ] 空状态是否说明了「为什么」和「下一步」？
- [ ] 错误提示是否说明了「可做什么」？
- [ ] 按钮文案是否用动词开头？
- [ ] 整体语气是否自然、专业、有品质感？

## 与其他规则的关系

| 规则 | 关系 |
|------|------|
| `client-ui-guidelines.mdc` §界面文案 | 本规则是其升级版，本规则优先 |
| `onboarding.mdc` §发版文案 | 本规则覆盖 onboarding 文案标准 |
| `desktop-release.mdc` §文案禁写 | 本规则覆盖更新日志文案标准 |
| `documentation.mdc` §更新日志 | 本规则是其 UI 侧补充 |

## 反模式

- ❌ 把技术日志直接展示给用户
- ❌ 用「成功/失败」替代具体结果描述
- ❌ 空状态只写「暂无数据」不给上下文
- ❌ 错误提示只说「出错了」不给行动建议
- ❌ 按钮用「确认/提交」替代具体动作
- ❌ 暴露 Provider 名、API 名、组件名
- ❌ 用「加载中」替代「正在加载 XX」
- ❌ 用否定句式（「不要」「不能」「禁止」）替代正向引导
