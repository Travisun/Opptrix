---
description: macOS 桌面打包签名、公证顺序与 mac-sign-checklist 维护（防漏签）
globs: apps/desktop/scripts/after-pack*.cjs,apps/desktop/scripts/after-sign*.cjs,apps/desktop/scripts/lib/mac-sign-checklist.cjs,apps/desktop/resources/mac-sign-checklist.json,apps/desktop/package.json,.github/workflows/release-desktop.yml,docs/DESKTOP-RELEASE.md
alwaysApply: false
---

# macOS 桌面签名 / 公证（硬性）

改 `afterPack` / `afterSign`、Playwright/Chromium、python bundle、`.dylib` / `.node`、或正式 mac 发版签名时 **必须**遵守本规则。发版总流程见 `desktop-release.mdc`；操作说明见 `docs/DESKTOP-RELEASE.md`。

---

## 流水线顺序（硬性）

正式 Developer ID 包 **唯一**允许的顺序：

```
afterPack（叶子 Mach-O 预签 + heavy trees stash）
  → electron-builder sign（build.mac.notarize: false）
  → afterSign（stash restore + 外层 .app reseal，禁止 --deep）
  → @electron/notarize + stapler validate
  → 再打 dmg / zip
```

- **禁止**依赖 electron-builder 在 `afterSign` 之前公证（EB 26 默认会抢跑；须保持 `build.mac.notarize: false`，由 `after-sign-restore-heavy.cjs` 在 restore+reseal 之后公证）。
- Ticket 必须对应当前最终 `.app`；stash 未 restore 或外层未 reseal 就公证 → 废票 / Gatekeeper 失败。

---

## 叶子先签（禁止 deep-only）

Playwright / Chrome nested `*.framework/.../Libraries/*.dylib`（如 libEGL、libGLESv2、libvk_swiftshader）等：

- **必须**叶子 Mach-O 先 `codesign`（Developer ID + timestamp），再 deepest-first 封嵌套 bundle。
- **禁止**只靠 `codesign --deep` 指望嵌套 Libraries 被正确签上。

---

## 签名清单（单一事实源）

| 项 | 说明 |
|----|------|
| 真源 | `apps/desktop/resources/mac-sign-checklist.json` |
| 用途 | `mustVerify` 路径列表；打包后硬失败校验防漏签 |
| 逻辑 | `apps/desktop/scripts/lib/mac-sign-checklist.cjs` |

改 Playwright / Chromium 版本、新增 `.dylib` / `.node`、改 python bundle 布局时：**必须先更新清单**，再打包 / 打标签。清单过期 = 漏签漏检。

---

## EMFILE 与外层 reseal

- Heavy trees（`python/`、`runtime-stage/node_modules/`、`runtime-stage/playwright-browsers`）在 afterPack **预签后 stash**，避免 `@electron/osx-sign` walk 触发 EMFILE。
- afterSign **restore** 后再 reseal 外层 `.app`。
- 外层 reseal **禁止 `--deep`**（嵌套树已预签；deep 会重走大树并破坏已有 seal / 再撞 EMFILE）。

---

## 验收

| 场景 | 要求 |
|------|------|
| 正式包 | `spctl --assess -vv --type execute Opptrix.app` 应为 **`Notarized Developer ID`** |
| CI 公证 Invalid / 拒签 | **先查** `mustVerify` 与清单是否过期、叶子是否未签、是否在 restore 前公证 |
| `OPPTRIX_MAC_UNSIGNED=1` | 仅本地/调试；**禁止**当正式发布产物 |

---

## 禁止（反模式）

- ❌ stash 未 restore（或外层未 reseal）就公证 / 打 dmg
- ❌ 只靠 `codesign --deep` 签 Playwright/Chrome nested Libraries
- ❌ 改二进制布局却不更新 `mac-sign-checklist.json`
- ❌ 把 `UNSIGNED` / ad-hoc / 未公证包当正式发布
- ❌ 打开 `build.mac.notarize: true` 让 builder 在 afterSign 前公证
- ❌ 外层 reseal 使用 `--deep`
