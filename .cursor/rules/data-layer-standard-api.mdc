---
description: 数据层标准 API 优先 — 新功能/改动前必查已有能力，禁止绕过标准层直连 Provider
globs:
  - packages/a-stock-layer/**/*
  - packages/research-hub/**/*
  - apps/server/**/*
  - client-ui/src/api/**/*
  - client-ui/src/market/**/*
alwaysApply: false
---

# 数据层标准 API 优先（强制）

任何**新功能、代码调用、Hub 接口、前端数据层**改动之前，必须先对照现有数据兼容架构，**套用已有标准方法**；标准能力不满足时，再按层级**扩展标准层**，禁止直接对接 Provider / 上游 API「重复造轮子」。

## 动手前必读（按顺序）

1. `docs/PROVIDER-STANDARD-API.md` — 三层边界与能力表
2. `docs/DATA-LAYER.md` — Engine / Registry / Hub 总览
3. `packages/a-stock-layer/src/core/instrument-query.ts` — `resolveInstrumentQueryPlan`
4. CodeGraph：查 `queryInstrumentData`、`InstrumentRef`、`inferCnAssetClass` 及目标 capability 的现有调用链

## 唯一标准入口

| 层级 | 用法 |
|------|------|
| Hub / Agent / 同步 | `engine.queryInstrumentData(ref, capability, opts?)` |
| 非标能力（最后手段） | `engine.invokeCustomMethod(providerId, method, args)` + 文档登记 |
| 标的引用 | `InstrumentRef`（`market` + `assetClass` + `symbol`）；A 股用 `inferCnAssetClass`，**禁止**把指数/ETF 一律当 `EQUITY` |

标准能力：`realtime` `kline` `snapshot` `profile` `financials` `stock_list` `instrument_search` `sector_list` `etf_*` 等（见 `InstrumentDataCapability`）。

## 扩展顺序（不得跳级）

1. **复用**：现有 capability + 既有 `opts`（`period` / `count` / `market`）能否满足需求？
2. **扩计划**：改 `instrument-query.ts` 的 `resolveInstrumentQueryPlan`（及必要时 `InstrumentDataCapability`）
3. **扩 Provider 标准方法**：在对应 Provider 实现与 capability 绑定的标准方法名（`kline`、`indexKline` 等经 Registry 路由，不私造 Hub 专用 fetch）
4. **自定义方法**：仅当无标准 capability 可映射；须 `custom-method-docs.ts` + `custom-methods.ts`（见 `data-provider-docs.mdc`）

## 禁止（反模式）

- Hub / `client-ui` / Agent **新增** `de.realtime()`、`de.kline()`、`invokeCustomMethod('tencent', …)` 等主路径直连（存量技术债应逐步收敛，不新增）
- 在 Hub 内手写 `tencentXxx` / `zzshareXxx` 聚合，而不经 `research-hub` → `queryInstrumentData` / `instrument-router`
- 指数、ETF、个股混用错误 `assetClass` 或错误 `ts_code`/secid 映射
- 已有标准 `instrument_search` / `kline` 再登记同义自定义方法

## 改动自检

- [ ] 已查 CodeGraph / 标准能力表，确认无法复用现有路径
- [ ] 新 Hub feature 映射到 `InstrumentDataCapability` 或已登记的自定义方法
- [ ] `InstrumentRef` 与 `assetClass` 正确（指数 `INDEX`、ETF `ETF`、个股 `EQUITY`）
- [ ] Provider 改动时 `manifest` binding 与 `data-provider-docs.mdc` 同步
- [ ] 未在 UI 层绕过 `research` API 直连第三方行情 URL

Provider 实现细则见 `.cursor/rules/provider-standard-api.mdc`。
