---
description: 测试规范 — 单元测试、集成测试、端到端测试
globs:
  - packages/**/*
  - apps/**/*
  - tests/**/*
alwaysApply: false
---

# 测试规范

## 测试金字塔

```
         ┌─────────┐
         │  E2E    │  少量关键路径
         │ (5%)    │  端到端验证
         ├─────────┤
         │集成测试  │  模块间交互
         │ (25%)   │  Provider/Hub/API
         ├─────────┤
         │单元测试  │  函数/类独立验证
         │ (70%)   │  核心逻辑
         └─────────┘
```

## 单元测试

### 文件位置

```
tests/                          ← 正式 CI 套件（node:test，*.test.mjs）
├── smoke.test.mjs
├── helpers.mjs                 ← waitForUrl / liveNetworkTestsEnabled 等
└── ...
packages/*/src/**/*.ts          ← 实现；勿再遗留未接入的 vitest 目录
```

手动 Provider 探针（需密钥/外网，不进 CI）：`packages/a-stock-layer` 的 `test:zzshare` / `test:tickflow` / `test:free-gaps`。

### 测试模式

```javascript
// ✅ Arrange-Act-Assert — node:test
import { describe, it } from 'node:test'
import assert from 'node:assert/strict'

describe('CircuitBreaker', () => {
  it('trips after 3 consecutive failures', () => {
    const breaker = new CircuitBreaker({ threshold: 3 })
    breaker.recordFailure()
    breaker.recordFailure()
    breaker.recordFailure()
    assert.equal(breaker.state, 'OPEN')
  })
})
```

### Mock 策略

```javascript
// ✅ Mock 网络：优先依赖注入 / 假 driver；必要时 stub fetch
import assert from 'node:assert/strict'
```

Live 上游默认关闭：

```javascript
import { liveNetworkTestsEnabled } from './helpers.mjs'
const LIVE = liveNetworkTestsEnabled() // OPPTRIX_LIVE_NETWORK_TESTS=1
test('…', { skip: !LIVE }, async () => { /* real upstream */ })
```

## 集成测试

### 数据库测试

```typescript
// ✅ 正确：使用临时数据库
describe('UserDataStore', () => {
  let store: UserDataStore
  
  beforeEach(() => {
    // 使用临时目录
    const tmpDir = fs.mkdtempSync(path.join(os.tmpdir(), 'opptrix-test-'))
    process.env.OPPTRIX_DATA_DIR = tmpDir
    store = UserDataStore.getInstance()
  })
  
  afterEach(() => {
    store.close()
    fs.rmSync(process.env.OPPTRIX_DATA_DIR!, { recursive: true })
  })
  
  it('should persist and retrieve documents', () => {
    store.setDocument('test', 'key1', { value: 'hello' })
    const result = store.getDocument('test', 'key1')
    expect(result).toEqual({ value: 'hello' })
  })
})
```

### Schema 迁移测试

```typescript
// ✅ 正确：测试跨版本迁移
describe('Schema Migration', () => {
  it('should migrate from v8 to v9', () => {
    const db = createTestDatabase()
    seedDatabaseToVersion(db, 8)  // 种子数据到 v8
    
    const store = new MarketDataStore(db)
    store.migrate()
    
    expect(store.detectAppliedSchemaVersion()).toBe(9)
    expect(db.prepare('SELECT * FROM stocks').all()).toHaveLength(10)
  })
  
  it('should be idempotent', () => {
    const db = createTestDatabase()
    seedDatabaseToVersion(db, 9)
    
    const store = new MarketDataStore(db)
    store.migrate()
    store.migrate()  // 重复迁移
    
    expect(store.detectAppliedSchemaVersion()).toBe(9)
  })
})
```

## 端到端测试

### API 测试

```typescript
// ✅ 正确：测试完整请求流
describe('API /api/stock/:code', () => {
  it('should return stock data', async () => {
    const response = await fetch('http://127.0.0.1:8711/api/stock/600519')
    const data = await response.json()
    
    expect(data.success).toBe(true)
    expect(data.data).toHaveProperty('code', '600519')
  })
  
  it('should return 400 for invalid code', async () => {
    const response = await fetch('http://127.0.0.1:8711/api/stock/invalid')
    expect(response.status).toBe(400)
  })
})
```

## 测试运行

```bash
# 单元测试
npm run test

# CI 等价（默认不上外网 live 探针）
npm run test:ci

# 含真实上游探针（腾讯/新浪等）
npm run test:live-network

# 特定文件
node --test --test-force-exit tests/smoke.test.mjs
```

## 测试覆盖率

### 目标

| 类型 | 覆盖率目标 |
|------|------------|
| 核心逻辑（Engine/Provider） | >80% |
| Hub 调度 | >70% |
| API 路由 | >60% |
| UI 组件 | 关键路径 |

### Runner

正式套件入口是 `scripts/run-tests.mjs`（`node:test`）。外网上游探针默认跳过，设 `OPPTRIX_LIVE_NETWORK_TESTS=1` 开启。

## 测试反模式

- ❌ **测试实现而非行为**：测试内部函数而非公共 API
- ❌ **过度 Mock**：Mock 了被测系统的核心依赖
- ❌ **测试间依赖**：测试 A 的结果影响测试 B
- ❌ **脆弱测试**：依赖特定执行顺序或时间
- ❌ **无断言测试**：测试运行但不验证结果
- ❌ **默认 CI 打真实上游**：live 探针应 opt-in，避免拖慢与抖失败