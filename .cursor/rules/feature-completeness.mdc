---
description: 新功能完备性与高可信交付 — 技术/用户/产品三维完备、兼容审计、禁止半成品
alwaysApply: true
---

# 新功能完备性与高可信交付（硬性）

## 核心原则

1. **禁止半成品**：新增/扩展功能必须可端到端使用；缺关键路径、缺失败态、缺兼容或未审计，不得标完成
2. **三维完备**：同一功能须同时从**技术**、**用户**、**产品**角度设计与验收
3. **先兼容，再扩展**：必须挂接现有分层/注册/标准 API；不得平行另起一套破坏其他功能
4. **引入 → 再审计 → 回炉**：实现后必须按本规则复审；不合格则补充/优化/测试/重做，直至达标
5. **方向性问题问用户**：范围、取舍、破坏性兼容、安全边界不清时先问，禁止静默拍板

## 触发条件

满足任一即强制适用（含「小改」若实质引入新能力）：

- 新增 feature / Hub / Provider / Agent tool / Schema / UI 主路径 / 桌面能力
- 审计、补齐、重构「未完成」功能
- 改动 ≥2 文件或跨 ≥2 架构层，且改变用户可感知行为

纯 typo、纯文案、只读问答：豁免。

## 强制工作流

```
Clarify（方向问用户）→ Design（三维 + 外部参照）→ Implement（完整路径）
  → Verify（门禁/测试）→ Re-audit（本清单）→ 不合格则回炉 → 达标才交付
```

与 `multi-agent-orchestration.mdc` 配合时：Explorer/Architect 覆盖 Design；Implementer 禁止交半成品；**Verifier 必须按下方清单逐条验**，未过不得交付。

## 设计前（必做）

| 维度 | 必须想清楚 |
|------|------------|
| **产品** | 用户要达成的结果、非目标、成功标准、与现有流程的关系 |
| **用户** | 主路径、空态/加载/错误/权限不足、可逆性、文案（`ui-copy-standard`） |
| **技术** | 挂哪一层、注册点、数据流、失败与超时、性能、观测/日志脱敏 |
| **兼容** | 旧客户端/旧数据/旧 API；是否断代；回滚策略 |
| **安全** | 输入校验、密钥、IPC/CORS、最小权限（`security.mdc`） |
| **外部参照** | 至少对照 1 个可信开源/业界做法（架构或交互），说明采纳/舍弃原因 |

方向不清 → 用 `requirement-clarification.mdc` 提问；禁止边做边猜。

## 实现完备性（缺一不可）

- [ ] 主成功路径端到端接通（UI↔API↔Hub/Engine 等实际调用链）
- [ ] 失败/超时/空数据/部分配置：有用户可理解反馈与可采取动作
- [ ] 边界：超大输入、并发、重复提交、离线/弱网（按场景）
- [ ] 不破坏相邻功能；无「只改新路径、旧路径静默坏掉」
- [ ] 测试：关键路径 + ≥1 边界/失败；门禁（`check:ui` / `build:packages` 等）绿
- [ ] 文档：架构/API/Agent 变更已按 `doc-sync.mdc` 同步

## 交付前复审（Re-audit，硬性）

对照上表与 `review-audit.mdc` 再跑一轮。任一条未过 → **回炉**（补实现/改方案/加测），禁止「先合并再补」。

向用户交付须能一句话说明：

1. 三维完备如何覆盖  
2. 兼容与安全结论  
3. 如何验证（命令/场景）  
4. 已知限制（无则写无）

## 反模式（禁止）

- ❌ 只做 happy path / UI 壳 / 提示词一句带过，渲染或数据链未接好
- ❌ 图例与图形语义不一致、预览挂载重型组件等「未想副作用」
- ❌ 绕过标准层另起平行实现
- ❌ 未问用户就做破坏性或不可逆取舍
- ❌ Verifier 只看「编译过」或 Status:success，不按清单验
- ❌ 「以后再补」安全、兼容、空错态、测试

## 与其他规则

| 规则 | 关系 |
|------|------|
| `requirement-clarification.mdc` | 方向/取舍提问 |
| `architecture-principles.mdc` / `security.mdc` | 技术与安全底线 |
| `review-audit.mdc` / `post-edit-verification.mdc` | 门禁与审查条目 |
| `multi-agent-orchestration.mdc` | 拆分与独立 Verifier；AC 须引用本规则清单 |
| `ui-copy-standard.mdc` / `backward-compatibility.mdc` | 用户文案与数据兼容 |
| `cross-platform-compat.mdc` | OS 维度：三平台可运行或明确降级，禁止「只通本机」 |
