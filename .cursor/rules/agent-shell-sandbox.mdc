---
description: Agent 命令隔离施工标准 — 会话级 SRT、围栏内任意命令、双轴策略、资源生命周期与可信安全交付
globs: packages/agent-workspace/src/shell/**/*,packages/agent/src/mcp/workspace-tools.ts,packages/agent/src/tool-meta.ts,packages/agent/src/tool-packs.ts,docs/AGENT-GUIDE.md,docs/DESKTOP.md
alwaysApply: false
---

# Agent 命令隔离（Shell / SRT）施工要求（硬性）

> 凡改动 Agent 本地命令执行、沙箱、联网授权、`opptrix_run`、workspace shell 相关实现或文档，**必须**遵守本规则。  
> 对标：Claude Code（围栏内 auto-allow + SRT）、Codex（sandbox × approval 双轴）、Cursor（同工具升权）；引擎继续 `@anthropic-ai/sandbox-runtime`。

## 已锁定的产品决策（2026-08 评审确认）

| # | 决策 | 施工含义 |
|---|------|----------|
| 1 | 入参仅 **`command` 字符串**（真 shell） | 不以 `argv[]` 白名单为主 API；内部可再解析，但工具契约是 `command` |
| 2 | **本对话首次跑命令即带安装源级网络** | 会话默认 `allowedDomains` 含官方/镜像包源集合；其它域名仍按需审批 |
| 3 | **无「首次运行命令」总确认** | 围栏内自动放行；删除主路径「允许运行命令」sticky |
| 4 | **`unsandboxed` 允许但每次确认** | 禁止「本对话一律」出围栏；仅 once |
| 5 | **完整 / 基础隔离均允许任意命令** | `unelevated` 必须在结果/状态中标明路径保护较弱 |
| 6 | **几乎不靠软拒绝黑名单** | 安全边界以 SRT FS/网络 + grant 为准；不为「感觉安全」恢复二进制白名单 |
| 7 | **主路径只保留 `opptrix_run`** | `opptrix_install` / `request_shell_network` **废弃为兼容别名或移除**；文档与路由不再推荐 |
| 8 | **SRT 复用提效** | 同 config 指纹复用；**仅** `initialize`/`reset` 全局串行；同 config 下 spawn 可并行 |
| 9 | **本版一次做齐** | 会话级 SRT + 任意命令 + 双轴/升权，禁止拆成「先半成品白名单」 |
| 10 | **用户可见说明须清晰** | 写明：在隔离环境中运行，且仅限已授权文件夹 |

未列项不得擅自改口；变更须重新需求确认（`requirement-clarification.mdc`）。

---

## 核心原则（可信软件）

1. **最小权限**：默认 `workspace_write` + 安装源级出站；其它主机与出围栏显式升权  
2. **纵深防御**：Grant（业务）+ SRT OS 围栏（强制）+ 审批（越界）；**禁止**用二进制白名单冒充路径安全  
3. **单一事实源**：会话策略与 SRT 句柄集中在 `SessionShellRuntime`（或等价），禁止多处 sticky 各记一套真相  
4. **失败可诊断**：沙箱拒绝须写回路径/域名与 `suggested_escalate`，禁止静默失败  
5. **生命周期闭合**：每个会话/句柄有明确 create → use → dispose；禁止泄漏代理、子进程、全局 SRT 脏状态  
6. **能耗与资源健康**：禁止每命令 `initialize`/`reset`；闲置与 `clearSession` 必须回收  

行业对齐（施工时自检，不写进用户文案）：最小权限、默认安全、纵深防御、安全失败（fail closed 于路径/未授权出站）、完整审计可观测（日志脱敏）、兼容升级不静默断代。

---

## 目标架构（必须实现）

```text
Agent → opptrix_run({ command, cwd/root, network?, escalate?, secret_refs? })
         → SessionShellRuntime(sessionId)
              policy: sandbox_mode × approval_policy
              approvals: hosts / unsandboxed once
              srt: configHash + handle（复用）
         → wrap + spawn（完整隔离）或 RestrictedToken（basic）
         → 统一结果 + suggested_escalate
clearSession / 进程退出 → dispose（reset SRT、杀未托孤儿、清 approvals）
```

### 双轴

| 轴 | 默认 | 说明 |
|----|------|------|
| `sandbox_mode` | `workspace_write` | grant 内可写；完整隔离走 SRT |
| `approval_policy` | `on_request` | 围栏内免问；越界（新域名 / unsandboxed）才问 |

`danger_full` / `escalate=unsandboxed`：每次人批，**不得** session sticky。

### 网络默认（决策 2）

- 会话创建 次 `opptrix_run` 起：`allowedDomains` **已含**包安装源集合（与现 `PACKAGE_INSTALL_ALLOWED_DOMAINS` + 当前 pip 镜像等价）  
- **不**等于全网放开；未在名单且未会话授权的 host 仍拒绝或触发域名确认  
- 运行中 `sandboxAskCallback` 仍可对未知 host 询问；批准后并入会话并可能触发 **一次** config rebuild

### 命令面（决策 1 + 6）

- 工具主参数：`command: string`（经沙箱 wrap 的 shell 执行）  
- cwd 必须落入 session grant；否则拒绝  
- **任意命令**在完整隔离下不得突破 grant/deny 路径（靠 SRT，不靠黑名单）  
- `python` / `node` / `npm`：保留托管运行时解析（改写到 active / Electron-as-node），作为**能力增强**而非唯一入口  
- 不恢复「仅 7 个二进制」主策略

### 废弃工具（决策 7）

- `opptrix_install` / `shell_install` / `request_shell_network`：兼容期内映射到 `opptrix_run` 等价语义或返回明确迁移提示；**Agent 指南与 tool-pack 主推荐只留 `opptrix_run`**  
- 禁止新代码依赖已废弃工具为主路径

---

## 会话级 SRT 与资源生命周期（硬性）

### 句柄

```ts
// 概念模型 — 实现须等价
SessionSrtHandle {
  configHash
  isolation: 'full' | 'basic'
  refCount / lastUsedAt
  dispose(): Promise<void>  // reset SRT（full）、释放代理相关、幂等
}
```

### 规则

| 要求 | 说明 |
|------|------|
| **禁止每命令 reset** | `run` 的 `finally` 不得无条件 `SandboxManager.reset()` |
| **复用** | 相同 `configHash` 复用已 initialize 的 SRT（跨同配置会话可共享底层，决策 8） |
| **串行边界** | **仅** `initialize` / `reset` / config 切换 走全局互斥；**spawn 不进全局大锁** |
| **config 变更** | 域名并入等导致 hash 变 → 该活跃配置串行 rebuild 一次，再继续 |
| **clearSession** | 必须 `dispose`：清 approvals、egress、secret access 联动、SRT 若无私有引用则 reset |
| **进程退出 / 服务销毁** | WorkspaceService 销毁路径必须回收全部 handle |
| **AbortSignal** | 超时与取消须杀掉子进程树，不留孤儿 |
| **幂等 dispose** | 重复 clear / dispose 不得抛未捕获异常 |
| **basic（unelevated）** | 不 initialize SRT；不得假装调用了完整围栏；结果带 `isolation: 'basic'` |
| **密钥** | full：sentinel；basic：拒绝明文注入（保持现状） |

### 健康与能耗

- 禁止忙等轮询 SRT 状态  
- 长超时有上限；默认超时与截断 stdout/stderr 保留  
- 避免无界会话 handle Map；会话删除必须删除条目  
- 日志：记 sessionId / hash / duration / isolation，**禁止**记密钥与完整环境变量  

---

## 安全施工清单（实现与评审必过）

- [ ] 路径突破：完整隔离下，grant 外读/写失败（测试覆盖）  
- [ ] 未授权域名：默认不可达；安装源集合可达；会话批准后可达  
- [ ] `unsandboxed`：无 session 级「一律允许」  
- [ ] SSRF / LAN：与现 `assertEgressHostGrantable` / LAN 开关一致  
- [ ] 用户数据根、homedir、`.ssh` 等 deny 仍在  
- [ ] 包全局安装（`-g` 等）策略：若仍保留须有测试；若改为纯围栏依赖须文档说明  
- [ ] Windows：elevated / unelevated 双路径均测或明确 skip；文案标明 basic 弱路径保护  
- [ ] macOS / Linux / Windows 成对（`cross-platform-compat.mdc`）  
- [ ] 旧客户端：废弃工具兼容别名或可读错误，不静默断代（`backward-compatibility` 精神）  

---

## Agent / UI / 文档

- 结果含：`isolation`、`blocked_by?`、`suggested_escalate?`；兼容过渡期可保留 `needs_network_egress`  
- 升权：**同工具**改 `network` / `escalate` / 扩大 command 侧意图，禁止再引导已废弃预批工具为主路径  
- 用户文案（`ui-copy-standard.mdc`）：说明「在隔离环境中运行，仅限你已授权的文件夹」；基础隔离须提示保护较弱  
- 同步：`docs/AGENT-GUIDE.md`、`docs/DESKTOP.md`、tool-meta / packs / 路由测试  

---

## 验证门禁（Verifier AC 须含）

1. 同 config 连续多次 `opptrix_run`：仅必要次数 initialize；无每命令 reset  
2. 围栏内本地命令：零「运行命令」弹窗  
3. 会话默认可访问安装源域名；任意第三方域名仍需批或失败带指引  
4. `clearSession` 后无 handle / 无脏 SRT（再跑会重新 initialize）  
5. Abort/超时无孤儿进程（抽测）  
6. 相关单测 + `build:packages`；涉及 UI 则 `check:ui`  
7. 文档与废弃工具说明已更新  

---

## 反模式（禁止）

- ❌ 每命令 `SandboxManager.reset()`  
- ❌ 全局 mutex 包住整个 spawn  
- ❌ 用二进制白名单当「防路径突破」  
- ❌ 多层 sticky（shell-run + install + egress）各为事实源  
- ❌ `unsandboxed` 会话级永久放行  
- ❌ basic 隔离却对用户声称「完整隔离」  
- ❌ 半成品：只做会话级 SRT 仍白名单，或只放开命令仍每命令 reset  
- ❌ 不 dispose、不清 Map、不杀子进程  
- ❌ 把技术术语（SRT、Seatbelt、WFP）写进用户主文案  

---

## 与其它规则

| 规则 | 关系 |
|------|------|
| `security.mdc` | 密钥、校验、Electron；本规则专管命令隔离生命周期 |
| `cross-platform-compat.mdc` | 三平台沙盒/降级 |
| `feature-completeness.mdc` | 禁止半成品；本规则 §验证门禁并入 Verifier |
| `mcp-tool-pack-routing.mdc` | 废弃工具下线与 `opptrix_run` 路由 |
| `ui-copy-standard.mdc` | 隔离说明的用户文案 |
| `doc-sync.mdc` | AGENT-GUIDE / DESKTOP 同步 |
| `multi-agent-orchestration.mdc` | 实现本能力时强制拆分 + 独立 Verifier，AC 引用本清单 |
