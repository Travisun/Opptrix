---
description: Cursor Hooks 索引 — 完整脚本在 ~/.projects-rules/Opptrix/.cursor/hooks/
alwaysApply: false
---

# Cursor Hooks 索引

| 文件 | 本地完整路径 |
|------|----------------|
| `ensure-codegraph.sh` | `~/.projects-rules/Opptrix/.cursor/hooks/ensure-codegraph.sh` |
| `hooks.json`（参考） | `~/.projects-rules/Opptrix/.cursor/hooks.json` |

## 仓库内运行方式

- `.cursor/hooks.json` 调用仓库内 **薄包装** `.cursor/hooks/ensure-codegraph.sh`
- 包装脚本 `exec` 上述本地完整脚本（含本机路径逻辑）

维护 hooks 时只改 `~/.projects-rules/Opptrix/.cursor/hooks/`，勿在仓库内写业务逻辑。
