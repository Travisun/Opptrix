# Dependencies
node_modules/
client-ui/node_modules/

# Build output
dist/
**/dist/
client-ui/dist/
*.tsbuildinfo

# Python (legacy — removed from repo)
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
# Root Python venv only — do not ignore packages/*/src/env (source modules)
/env/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Secrets & runtime data
.env
.env.*
!.env.example
compose.env
.opptrix.json
.opptrix-host.json
packages/selfhost/bundle/
*.key
certs/
!apps/desktop/electron/certs/
!apps/desktop/electron/certs/**
api_keys.txt
apps/server/data/

# MiMoCode local secrets / credentials (keep skills + AGENTS.md)
.mimocode/**/.env
.mimocode/**/.env.*
.mimocode/**/auth.json
.mimocode/**/*credentials*
.mimocode/**/*secret*
.mimocode/**/*api*key*
.mimocode/node_modules/
.mimocode/package-lock.json
.mimocode/bun.lock

# User-local state (portfolio, market data, sessions)
.opptrix/
.a_stock_layer/

# Local LLM weights (place HY-MT GGUF here; not committed)
apps/server/llms/*.gguf
llms/*.gguf

# Desktop bundled SenseVoice GGUF (staged at build time)
apps/desktop/resources/sensevoice/*.gguf

# Desktop bundled e5 / RapidOCR (staged at build time; onnx/json not in git)
apps/desktop/resources/llms/
# Desktop bundled Python RAG engine wheels (staged at build time)
apps/desktop/resources/engines/
# Desktop bundled managed Python runtime (staged at build time)
apps/desktop/resources/python/
# Legacy stage path (pre-llms unification)
apps/desktop/resources/models/

# Logs & cache
logs/
data/cache/
outputs/

# CodeGraph / Cursor / MiMoCode — never track; keep entirely local
.codegraph/
.cursor/
.mimocode/

# Reasonix tool state / permissions cache — never track
.reasonix/
reasonix.toml

# Runtime pack output (scripts/pack-opptrix-runtime.mjs test runs)
dist-runtime/
opptrix-runtime.json

# Local trials when using example/ configs
example/runtime-local/
.akshare-ref/

# Agent/MiMoCode generated files
.agents/
skills-lock.json

# local prepare:fonts debug
.prepare-fonts-out.tmp


# docs/ — never track; keep entirely local so pull/checkout cannot clobber working copies
/docs/